import Link from 'next/link'; import type {Product} from '../lib/types';
export default function ProductCard({p}:{p:Product}){const status=p.stock>10?'TERSEDIA':p.stock>0?'STOK TERBATAS':'HABIS';return <article className="card"><Link href={`/catalog/${p.id}`}><img className="product-image" src={p.image} alt={p.name}/></Link><div className="card-body"><span className="tag">{p.categoryName}</span><h3 style={{marginTop:10}}>{p.name}</h3><p className="muted" style={{lineHeight:1.55}}>{p.description}</p><div className="price">Rp {Number(p.price).toLocaleString('id-ID')}</div><div className={`stock ${p.stock===0?'out':p.stock<=10?'low':'ok'}`} style={{marginTop:8}}>{status} · {p.stock} {p.unit}</div></div></article>}
