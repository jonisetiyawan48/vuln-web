'use client'; import {useRouter} from 'next/navigation'; import {useState} from 'react';
export function ProductForm({categories,initial}:{categories:any[];initial?:any}){const router=useRouter();const [f,setF]=useState({categoryId:initial?.categoryId||categories[0]?.id||'',name:initial?.name||'',description:initial?.description||'',image:initial?.image||'',price:initial?.price||0,unit:initial?.unit||'',stock:initial?.stock||0,active:initial?.active!==0});const set=(k:string,v:any)=>setF(x=>({...x,[k]:v}));return <form className="form" onSubmit={async e=>{e.preventDefault();await fetch(initial?`/api/admin/products/${initial.id}`:'/api/admin/products',{method:initial?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});router.push('/admin/products');router.refresh()}}>{[['name','Nama produk'],['image','URL gambar'],['unit','Satuan']].map(([k,l])=><div className="field" key={k}><label>{l}</label><input className="input" value={f[k as keyof typeof f] as any} onChange={e=>set(k,e.target.value)} required/></div>)}<div className="field"><label>Kategori</label><select className="select" value={f.categoryId} onChange={e=>set('categoryId',Number(e.target.value))}>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></div><div className="field"><label>Deskripsi</label><textarea className="textarea" rows={5} value={f.description} onChange={e=>set('description',e.target.value)} required/></div><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:12}}><div className="field"><label>Harga</label><input className="input" type="number" value={f.price} onChange={e=>set('price',Number(e.target.value))}/></div><div className="field"><label>Stok</label><input className="input" type="number" value={f.stock} onChange={e=>set('stock',Number(e.target.value))}/></div></div><button className="btn btn-primary" type="submit">Simpan produk</button></form>}
export function CategoryForm({initial}:{initial?:any}){const router=useRouter();const [name,setName]=useState(initial?.name||'');const [description,setDescription]=useState(initial?.description||'');return <form className="form" onSubmit={async e=>{e.preventDefault();await fetch(initial?`/api/admin/categories/${initial.id}`:'/api/admin/categories',{method:initial?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,description})});router.push('/admin/categories');router.refresh()}}><div className="field"><label>Nama kategori</label><input className="input" value={name} onChange={e=>setName(e.target.value)} required/></div><div className="field"><label>Deskripsi</label><textarea className="textarea" rows={5} value={description} onChange={e=>setDescription(e.target.value)}/></div><button className="btn btn-primary">Simpan kategori</button></form>}
