'use client'; import {useRouter,useSearchParams} from 'next/navigation'; import {useState} from 'react';
export default function SearchBox({initial=''}:{initial?:string}){const router=useRouter();const sp=useSearchParams();const [q,setQ]=useState(initial);return <form className="search" onSubmit={e=>{e.preventDefault();const p=new URLSearchParams(sp.toString());q?p.set('q',q):p.delete('q');router.push(`/catalog?${p.toString()}`)}}><input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="Cari nama atau deskripsi produk..."/><button className="btn btn-primary">Cari</button></form>}
