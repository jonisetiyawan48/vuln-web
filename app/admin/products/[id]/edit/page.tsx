import {getProduct,listCategories} from '../../../../../lib/catalog'; import {ProductForm} from '../../../../../components/AdminForm'; import {notFound} from 'next/navigation'; export default async function Edit({params}:{params:Promise<{id:string}>}){const {id}=await params;const p=(await getProduct(Number(id)))[0];if(!p)notFound();return <><div className="eyebrow">Produk</div><h2>Edit produk</h2><ProductForm categories={await listCategories()} initial={p}/></>}
