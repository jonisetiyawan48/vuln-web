import {listCategories} from '../../../../../lib/catalog'; import {CategoryForm} from '../../../../../components/AdminForm'; import {notFound} from 'next/navigation'; export default async function Edit({params}:{params:Promise<{id:string}>}){const {id}=await params;const c=(await listCategories()).find(x=>x.id===Number(id));if(!c)notFound();return <><div className="eyebrow">Kategori</div><h2>Edit kategori</h2><CategoryForm initial={c}/></>}
