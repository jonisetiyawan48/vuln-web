import Link from 'next/link'; import {listCategories} from '../../../lib/catalog'; import DeleteButton from '../../../components/DeleteButton'; export default async function Categories(){const cats=await listCategories();return <><div className="section-head"><div><div className="eyebrow">Manajemen</div><h2>Kategori</h2></div><Link href="/admin/categories/new" className="btn btn-primary">+ Kategori baru</Link></div><div className="grid" style={{gridTemplateColumns:'repeat(3,1fr)'}}>{cats.map(c=><div className="card" key={c.id}><div className="card-body"><span className="tag">{c.productCount} produk</span><h3 style={{marginTop:10}}>{c.name}</h3><p className="muted">{c.description}</p><div style={{display:'flex',gap:8}}><Link className="btn btn-ghost" href={`/admin/categories/${c.id}/edit`}>Edit</Link><DeleteButton url={`/api/admin/categories/${c.id}`}/></div></div></div>)}</div></>}
