import {NextRequest,NextResponse} from 'next/server'; import {adminProducts} from '../../../../lib/catalog'; import {createProduct} from '../../../../lib/admin'; export async function GET(req:NextRequest){return NextResponse.json(await adminProducts(req.nextUrl.searchParams.get('q')||''))} export async function POST(req:NextRequest){try{const id=await createProduct(await req.json());return NextResponse.json({id},{status:201})}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
